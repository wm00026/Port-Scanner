# Port-Scanner
A basic port scanner, written in C

The program takes the given IP and a range of ports and scans each one

To run, grab the file and compile it. For example, using GCC:
```sh

gcc scanner.c -o <file-name>

```

Then, run the executable:
```sh

./scanner.out <ip> <first_port> <last_port> 

```
